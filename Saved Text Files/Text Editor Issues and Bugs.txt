// Fixed  When I only use find and replace then exit a file, it doesn't warn me about unsaved changes.
// Fixed  When clicking on edit or view in the menu bar, the shortcut description and the shortcut are sometimes overlapping.
// Fixed  To zoom in I have to press ctrlShift+ instead of just ctrl+.
// Fixed  When I use Replace All it clears the undo history and I can't undo it.

// Fixed  When first opening the text editor it goes off the screen a little until it is repositioned.
// Fixed  When zooming in or out doesn't change the size of the line numbers and they look really bad.
Can't delete files or folders in the side bar.
Can't drag and drop a file or folder into another folder in the side bar.
// Fixed  Can't tell which folder I am in, in the side bar.
// Fixed  No shortcuts for create folder and open folder.

// Fixed  Currently can't change which folder is shown in the side bar.
If something is added to a file then removed, it still shows that the file needs to be saved, but that is what vs code does too.
// Fixed  When zooming, it is impossible to tell if I am back at the default 100% or not.

Scroll bar doesn't look that great.